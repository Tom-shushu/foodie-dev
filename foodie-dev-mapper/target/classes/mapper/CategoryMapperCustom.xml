<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhouhong.mapper.CategoryMapperCustom">
  
  <resultMap id="myCategoryVO" type="com.zhouhong.pojo.vo.CategoryVO">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="name"  property="name" />
    <result column="type"  property="type" />
    <result column="fatherId"  property="fatherId" />

    <!--
        collection: 标签：用于定义关联的list集合的封装规则
        property： 对应三级分类的list属性名
        ofType： 集合的类型
    -->
    <collection property="subCatList" ofType="com.zhouhong.pojo.vo.SubCategoryVO">
      <id column="subId" jdbcType="INTEGER" property="subId" />
      <result column="subName"  property="subName" />
      <result column="subType"  property="subType" />
      <result column="subFatherId"  property="subFatherId" />

    </collection>
  </resultMap>
  
  <select id="getSubCatList" resultMap="myCategoryVO" parameterType="int">
    SELECT
       f.id AS id,
       f.`name` AS `name`,
       f.type AS `type`,
       f.father_id AS fatherId,
       c.name AS subName,
       c.type AS subType,
       c.father_id AS subFatherId
      FROM
      category f
      LEFT JOIN
      category c
      ON f.id = c.father_id
      WHERE
      f.father_id = #{rootCatId}
  </select>
</mapper>